{{ header }}
<div id="content">
  <div class="container">
    <div class="row justify-content-sm-center">
      <div class="col-12 auth-layout">
        <div class="auth-brand" aria-hidden="true"><span class="oc-brand-logo"></span></div>
        <div class="card card-md">
          <div class="card-body">
            <h2 class="h2 text-center mb-4">{{ heading_title }}</h2>

            <form id="form-forgotten" class="auth-form" action="{{ reset }}" method="post" data-oc-toggle="ajax">
              <p class="text-secondary mb-4">{{ text_password }}</p>

              <div class="mb-3">
                <label for="input-password" class="form-label">{{ entry_password }}</label>
                <input type="password" name="password" value="" id="input-password" class="form-control"/>
              </div>

              <div class="mb-3">
                <label for="input-confirm" class="form-label">{{ entry_confirm }}</label>
                <input type="password" name="confirm" value="" id="input-confirm" class="form-control"/>
              </div>

              <div class="form-footer">
                <button type="submit" class="btn btn-primary w-100">{{ button_save }}</button>
              </div>

              <div class="text-center text-secondary mt-3">
                <a href="{{ back }}">{{ button_back }}</a>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
{{ footer }}
