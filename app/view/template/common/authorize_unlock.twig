{{ header }}
<div id="content">
  <div class="container-fluid">
    <div class="row justify-content-sm-center">
      <div class="col-12 auth-layout">
        <div class="auth-brand" aria-hidden="true"><span class="oc-brand-logo"></span></div>
        <div class="card card-md">
          <div class="card-body">
            <h2 class="h2 text-center mb-4">{{ text_unlocked }}</h2>

            <p class="text-secondary mb-4">{{ text_unlock }}</p>

            <div class="form-footer">
              <a href="{{ login }}" class="btn btn-primary w-100">{{ button_login }}</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
{{ footer }}
