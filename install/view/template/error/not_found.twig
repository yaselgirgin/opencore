{{ header }}
<div id="error-not-found" class="container">
	<div class="card">
		<div class="card-body">
			<h1>{{ heading_title }}</h1>
			<p>{{ text_error }}</p>
			<a href="{{ continue }}" class="btn btn-primary">{{ button_continue }}</a>
		</div>
	</div>
</div>
{{ footer }}
